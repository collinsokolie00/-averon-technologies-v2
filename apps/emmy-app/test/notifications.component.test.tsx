import { afterEach, describe, expect, it, vi } from "vitest";
import { cleanup, fireEvent, render, screen } from "@testing-library/react";
import { MemoryRouter } from "react-router";
import type { WorkspaceNotification, WorkspaceSummary } from "@averon/shared-types";
vi.mock("../src/firebase", () => ({ firebaseAuth: { currentUser: null }, googleAuthProvider: {} }));
import { NotificationsView } from "../src/App";
afterEach(cleanup);
const workspaces: WorkspaceSummary[] = [{ workspace: { id: "averon", businessId: "averon", name: "Averon Technologies", slug: "averon", status: "active" }, business: { id: "averon", name: "Averon Technologies", slug: "averon", status: "active" }, role: "owner", permissions: [] }];
const items: WorkspaceNotification[] = [{ notificationId: "n1", workspaceId: "averon", businessId: "averon", category: "WEBSITE_HEALTH", severity: "warning", title: "Weekly health review", message: "One issue requires attention.", source: "workspace.health_review", createdAt: "2026-08-31T00:00:00.000Z", status: "unread", authoritativeStatus: "recommendation", recommendation: { observed: ["API evidence reported one failure."], suggested: ["Review runtime stability."] }, destination: { kind: "internal", path: "/diagnostics", label: "View diagnostics" } }];
describe("workspace operations inbox", () => { it("renders canonical identity, evidence polarity, safe action and read lifecycle", () => { const calls: unknown[][] = []; render(<MemoryRouter><NotificationsView items={items} workspaces={workspaces} loading={false} error="" onStatus={async (...args) => { calls.push(args); }}/></MemoryRouter>); expect(screen.getAllByText("Averon Technologies", { exact: false }).length).toBeGreaterThan(0); expect(screen.getByText(/Observed:/)).toBeTruthy(); expect(screen.getByText(/Suggested:/)).toBeTruthy(); expect(screen.getByRole("link", { name: "View diagnostics" }).getAttribute("href")).toBe("/diagnostics"); fireEvent.click(screen.getByRole("button", { name: "Mark read" })); expect(calls).toEqual([["n1", "read"]]); fireEvent.change(screen.getByLabelText("Filter by status"), { target: { value: "read" } }); expect(screen.getByText("You’re all caught up")).toBeTruthy(); }); });
