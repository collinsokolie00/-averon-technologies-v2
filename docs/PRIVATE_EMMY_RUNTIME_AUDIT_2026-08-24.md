# Private Emmy runtime audit — 2026-08-24

The authenticated owner sent the exact coordinated go-to-market prompt in workspace `averon`. Request `12fd033f-0e95-4ab8-ab03-cf7f3e454314` used conversation `O0UrfRnJz6NdxPFhbyjw`, returned HTTP 200 in 10,396 ms, completed the DeepSeek provider call and synthesis, emitted the completed stream event, triggered the frontend conversation refresh, rendered visibly, and persisted assistant message `PCe9QLU6IdWG5Ehqz9Am` with `generationStatus: completed` and 3,155 characters. The owner supplied the rendered response as acceptance evidence.

The request did not invoke specialists. Capability matching found Research, SEO, and Marketing, but the unchanged four-specialist-call budget conservatively accounts for each selected agent's schema-repair allowance. Research (maximum 2), SEO (maximum 2), and Marketing (maximum 1) total five possible specialist calls, so the smallest-team selector marked the capability set unresolved and private Emmy produced a bounded direct response. This is the designed Phase 16 budget boundary, not a provider, streaming, persistence, Business OS, or agent-registry failure. Limits and the 15-agent registry were not changed during this audit.

The controlled browser had no Firebase session. Authentication was not bypassed; the owner performed the single live action in the already-authenticated browser while API logs and Firestore metadata were correlated server-side.

## Grounded specialist acceptance run

After the reviewed public Business OS migration, the owner sent one controlled request asking Emmy to use authoritative Averon knowledge, identify external competitor-research gaps, and produce a grounded positioning and 90-day go-to-market strategy. The live response rendered in the authenticated app and reported that Research and Marketing were invoked. Research returned a deterministic missing-input result because no governed external browsing source is currently available. Marketing returned an invalid specialist result. The synthesis safety gate did not invent the missing competitive analysis; it returned the grounded Business OS facts, clearly separated evidence gaps, and bounded general guidance.

Firestore correlation proves canonical completion under workspace `averon`: conversation `SXDfqIfnSDjnRNEUGSXm` contains user message `8iMbyFu5ud4iO6pq2Efh` (319 characters) and assistant message `YGEGlgq70Oiquw8TELh1` (3,276 characters, three citations, `generationStatus: completed`). This proves the planner-to-specialist-to-synthesis path ran and persisted its final response. It does not prove both specialists produced valid completed results: Research correctly required unavailable external evidence, while Marketing requires a focused output-contract diagnosis before claiming full swarm quality for this scenario. No agent, budget, provider, authentication, or tenancy change was made in response to that result.

### Marketing schema diagnosis and correction

Diagnostic request `791ea169-9124-4c8a-8f00-db57a2e5ebb6` retained team size two, three provider calls, 6,545 tokens, three citations, valid attribution, and `WORKER_SCHEMA_INVALID` for the Marketing + Research run. A provider reproduction using synthetic company data proved that both the initial Marketing response and its controlled repair were valid JSON with the correct keys, but `findings`, `recommendations`, and `risks` contained objects. The `marketing.v1` contract intentionally requires arrays of plain strings. Its prompt represented those fields only as empty arrays, leaving the element type ambiguous, and the repair repeated the same ambiguity.

The schema instruction now supplies explicit plain-string examples and states that those arrays must never contain objects. Validation was not weakened and the schema version, provider, call budget, planner, registry, and agent count remain unchanged. The same synthetic provider acceptance completed in one call with all five Marketing output arrays containing strings and `validationStatus: valid`. A regression test covers conversion of the exact object-array failure into the existing string-array contract.

### Same-conversation retry defect and live acceptance

The first post-Marketing-fix retry (`d5270b2d-5485-427f-90e5-51ac93631192`) failed before private Emmy because the prior 3,276-character assistant response was copied unbounded into request history, while Emmy history validation permits 900 characters per item. The already-open NDJSON response consequently returned an `EMMY_INVALID_REQUEST` interrupted event with HTTP 200; no provider, planner, specialist, synthesis, diagnostic, or assistant persistence stage was reached. Conversation history is now bounded to 900 characters per item without modifying canonical stored messages. Regression coverage retries after a 3,276-character persisted response and proves a completed stream and assistant persistence.

Authenticated live acceptance request `1c94eb7b-104b-497e-a6d2-30c612ea243d` then completed in 21,026 ms. Marketing completed and validated in one provider call; Research returned the designed `needs_input` result for unavailable external research; orchestration completed `partial`; synthesis succeeded; the client requested its conversation-list refresh after consuming the completed event. Firestore contains user message `Yvgknc6fgejinrSiTGVu` and completed assistant message `rqDxbeTuYK9hB8A7yvLh` (8,040 characters). This is the successful live acceptance for the repaired Marketing and same-conversation streaming path.
